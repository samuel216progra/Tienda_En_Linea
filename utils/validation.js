const validateUsername = (username) => {
    // Implementa la lógica de validación del nombre de usuario
};

const validatePassword = (password) => {
   
};


module.exports = { validateUsername, validatePassword };
